apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: aitask

commonLabels:
  app.kubernetes.io/part-of: aitask-platform

resources:
  - namespace.yaml
  - configmap.yaml
  - secret.yaml
  - mongo-statefulset.yaml
  - redis-deployment.yaml
  - backend-deployment.yaml
  - worker-deployment.yaml
  - frontend-deployment.yaml
  - ingress.yaml
